export const environment = {
  production: true,
  apibase: '/', // value will be reduced to / if its serve on save url
  useBackend: true,
  updateUrl: 'https://localhost:5001/remote_server_apk/version.xml',
};
