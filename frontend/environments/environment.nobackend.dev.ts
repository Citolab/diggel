export const environment = {
  production: false,
  apibase: 'http://localhost:5000/', // localhost is replace by 10.0.2.2 if onCordova = true
  useBackend: false,
  updateUrl:
    'https://diggel.azurewebsites.net/remote_server_apk/version.xml',
};
